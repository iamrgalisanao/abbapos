{
  "name": "abbapos-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "core"
  ],
  "scripts": {
    "core:test": "npm test --workspace=core",
    "client:dev": "npm run dev --workspace=client",
    "server:dev": "cd server && php artisan serve"
  }
}
